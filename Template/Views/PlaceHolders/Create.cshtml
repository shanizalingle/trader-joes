@{
  Layout = "_Layout";
}

@model Template.Models.PlaceHolder

<h4>Add a new PlaceHolder</h4>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)
  <input type="submit" value="Add new placeHolder" />
}
<p>@Html.ActionLink("Show all placeHolders", "Index")</p>